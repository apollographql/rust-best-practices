interface:
  display_name: "Rust Code Review"
  short_description: "Review Rust invariants, ownership, and behavior"
  default_prompt: "Use $rust-code-review to review the current Rust changes against the repository's agent guide."
